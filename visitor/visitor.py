from abc import ABCMeta, abstractmethod

# There is no Overload in Python. so skip for now
# class Visitor(metaclass=ABCMeta):
#     @abstractmethod
#     def visit(self, file):
#         pass
#     @abstractmethod
#     def visit(self, directory):
#         pass
